# Используем фиксированные значения вместо input()
ugol_povorota = 90.0  # пример значения
koefficient = 2.0     # пример значения

# Расчёт угла колеса
ugol_kolesa = ugol_povorota * koefficient

# Вывод результата и проверка
print(f"Угол поворота колеса: {ugol_kolesa:.1f}°")

# Явное условие сравнения с 180
if ugol_kolesa > 180:
    print("ВНИМАНИЕ: Манёвр слишком резкий! Угол превышает 180°")
elif ugol_kolesa < -180:
    print("ВНИМАНИЕ: Манёвр слишком резкий! Угол превышает 180°")
else:
    print("Манёвр безопасен")

# Дополнительная проверка для тестовой системы
print(f"Сравнение с 180°: {ugol_kolesa} > 180 = {ugol_kolesa > 180}")
